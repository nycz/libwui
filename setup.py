from setuptools import setup, find_packages

setup(
    name='libwui',
    version='0.1',
    description='Library for easy argument parsing and cli format',
    url='https://github.com/nycz/libwui',
    author='nycz',
    python_requires='>=3.6',
    packages=find_packages(),
)
